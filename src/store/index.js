import { createStore } from 'vuex'
import post from './modules/post';

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        post
    }
})
