// STATE
const state = {
    isLogged: false,
}

// GETTERS
const getters = {
    getIsLogged: state => {
        return state.isLogged;
    }
}

// MUTATIONS
const mutations = {

}

// ACTIONS
const actions = {

}

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
}