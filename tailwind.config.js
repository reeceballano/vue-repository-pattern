module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {
            fontFamily: {
                sans: ['Quicksand'],
                // body: ['Quicksand']
            }
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
